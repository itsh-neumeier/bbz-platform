# deploy/quorum/.env — BBZ-QUORUM01 (etcd witness only).
# Copy to .env and fill in. NEVER commit the real .env.

BBZ_NODE_ID=BBZ-QUORUM01

# For a one-host local simulation, point at this host.
ETCD_ADVERTISE_HOST=127.0.0.1
ETCD_INITIAL_CLUSTER=BBZ-QUORUM01=http://127.0.0.1:2380
ETCD_INITIAL_CLUSTER_STATE=new
ETCD_CLUSTER_TOKEN=bbz-etcd
