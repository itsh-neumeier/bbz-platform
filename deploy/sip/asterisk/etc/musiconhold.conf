; BBZ SIP lab — music on hold (E13-12 / #817).
;
; The BBZ-generated classes ([bbz-moh-<id>]) are written by sync-trunk-config.sh
; into musiconhold_bbz.conf and the WAVs into /var/lib/asterisk/moh/bbz/<id>/.
; #tryinclude is silent when the file is absent (no MoH configured yet).

[general]

[default]
mode = files
directory = /usr/share/asterisk/moh
sort = alpha

#tryinclude musiconhold_bbz.conf
