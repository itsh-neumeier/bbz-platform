# Aggregated dev install for the Python workspace.
#   python -m venv .venv
#   .venv/Scripts/pip install -r requirements-dev.txt   (Windows)
#   .venv/bin/pip install -r requirements-dev.txt       (Linux/macOS)
#
# Runtime dependencies are pinned per component; this file wires the editable
# installs plus shared tooling.

-e ./packages/integration-sdk
-e ./packages/rule-dsl
-e ./packages/event-schemas
-e ./server

# Shared tooling
# Upper bounds are deliberately loose: security scanning (pip-audit) must be
# free to pull patched releases. Compatibility is caught by CI, not by pinning.
ruff>=0.16.4
mypy>=2.3.1,<3.0
import-linter>=2.13,<3.0
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
httpx>=0.28.1
pip-audit>=2.10.1
types-jsonschema>=4.26.0.20260518
