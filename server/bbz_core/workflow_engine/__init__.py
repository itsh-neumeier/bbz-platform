"""EPK-style workflow engine (ADR-0005).

Placeholder package. Phase 1+ implements the versioned graph runtime: EPK
Event/Function nodes, AND/OR/XOR split/join token semantics, publish-time graph
validation, and template-version pinning for running instances. Conditions use
the restricted rule DSL (``bbz_rule_dsl``), never ``eval``.
"""
