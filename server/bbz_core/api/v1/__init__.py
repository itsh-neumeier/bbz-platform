"""Versioned public API surface (``/api/v1``).

MASTER_PROMPT §15: REST for commands/queries, WebSocket/SSE for live updates.
Phase 0 exposes only ``/api/v1/meta`` plus the OpenAPI document so the contract
is versioned from the start.
"""
