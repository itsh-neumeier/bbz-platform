"""HTTP layer: routers, error model, request-scoped middleware.

This package may depend on ``bbz_core.domain`` and ``bbz_core.infra`` but NOT on
``bbz_integration_sdk`` (see import-linter contracts in the root pyproject).
"""
